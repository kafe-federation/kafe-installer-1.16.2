Bugs in simpleSAMLphp older than 1.14.14
- if a single SP logout, instead of single logout(SLO), all SPs are forced to logout.

Bugs in kafe_installer (fixed)
- iframe-logout.php (jquery --> jquery-1.8)
$this->data['head'] .= '<script type="text/javascript" src="/' . $this->data['baseurlpath'] . 'resources/jquery-1.8.js"></script>';
